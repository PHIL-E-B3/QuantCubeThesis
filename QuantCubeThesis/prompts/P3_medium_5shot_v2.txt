## TODO: Replace the 5 few-shot examples below with new sentences selected by research partner.
## The prompt template (field definitions, rules) stays the same — only the example sentences and their labels change.
## Current examples (IDs to replace):
##   1. e5418507 — boilerplate
##   2. 99122de6 — unemployment, level-vs-change
##   3. c7346ec3 — economic_activity, contested
##   4. cd96b673 — monetary_policy, strong dovish (-2)
##   5. 995fddd0 — inflation, risk upside
## After replacing, update EXAMPLE_IDS in sft_train.py and sft_eval.py to exclude the new IDs.

You are classifying sentences from FOMC communications. For each sentence, return a JSON object with 7 fields in this exact order: top, ten, sen, hor, com, ris, wid.

FIELD DEFINITIONS:

"top" (array): Topic(s) present. Values: "inflation", "unemployment", "economic_activity", "macro" (aggregate outlook, no single variable dominates), "financial_conditions", "monetary_policy", "boilerplate" (procedural/administrative), "no_topic". A sentence can have multiple topics.

"ten" (string): "descriptive" = reporting past/present data. "interpretive" = forward-looking, projections, conditionals, outlooks.

"sen" (integer or "na"): Hawkish/dovish policy incentive. +2/+1 = hawkish, -1/-2 = dovish, 0 = neutral. "na" only for boilerplate. Polarity rules: inflation rising = hawkish (+), unemployment falling/tight = hawkish (+), stronger growth = hawkish (+), financial conditions easier = hawkish (+) (counterintuitive: easier conditions mean the Fed must do more), monetary policy tightening = hawkish (+). IMPORTANT: when a sentence shows direction-of-change AND level (e.g. "fell but remained high"), label the direction of change, not the level. Use +2/-2 only for linguistically intensified language (surged, collapsed, significantly, well above) or actions of 50+ bps.

"hor" (boolean): true only if explicit long-term horizon language present ("for some time", "longer run", "over the medium term") AND ten = "interpretive". Otherwise false.

"com" (string): Applies ONLY to monetary_policy sentences. "unconditional" = explicit commitment, no stated condition. "conditional" = action contingent on data. "none" = all other cases and all non-monetary-policy topics.

"ris" (string): Directional tail risk. "skewed_downside" = risks weighted toward worse outcomes. "skewed_upside" = risks toward overheating/inflation overshoot. "symmetric" = explicitly balanced. "na" = no explicit risk framing.

"wid" (string): "elevated" = explicit acknowledgement of uncertainty ("highly uncertain", "difficult to assess", "hard to gauge"). "contested" = two genuinely opposing forces on the same topic netting to zero (NOT direction-of-change + level reference). "none" = baseline.

When top = "boilerplate": set sen = "na", com = "none", hor = false, ris = "na", wid = "none".

EXAMPLES:

Sentence: "REPLACE WITH NEW EXAMPLE 1"
{"top": ["LABEL"], "ten": "LABEL", "sen": LABEL, "hor": LABEL, "com": "LABEL", "ris": "LABEL", "wid": "LABEL"}

Sentence: "REPLACE WITH NEW EXAMPLE 2"
{"top": ["LABEL"], "ten": "LABEL", "sen": LABEL, "hor": LABEL, "com": "LABEL", "ris": "LABEL", "wid": "LABEL"}

Sentence: "REPLACE WITH NEW EXAMPLE 3"
{"top": ["LABEL"], "ten": "LABEL", "sen": LABEL, "hor": LABEL, "com": "LABEL", "ris": "LABEL", "wid": "LABEL"}

Sentence: "REPLACE WITH NEW EXAMPLE 4"
{"top": ["LABEL"], "ten": "LABEL", "sen": LABEL, "hor": LABEL, "com": "LABEL", "ris": "LABEL", "wid": "LABEL"}

Sentence: "REPLACE WITH NEW EXAMPLE 5"
{"top": ["LABEL"], "ten": "LABEL", "sen": LABEL, "hor": LABEL, "com": "LABEL", "ris": "LABEL", "wid": "LABEL"}

Now classify this sentence. Return only the JSON object, no other text.

Sentence: "{sentence}"

JSON: